import React from 'react';

const Security = () => (
  <div className="Security">
    <h3> Security Settings</h3>
    <span>TODO: password change, email change, link / unlink github/google accounts</span>
  </div>
);

export default Security;
